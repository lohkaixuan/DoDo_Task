1) clone from git
git clone https://github.com/lohkaixuan/dodotask-back.git
cd dodotask-back

2) create venv
python -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass  #if need
.\.venv\Scripts\Activate.ps1

3) install dep
pip install -r requirement.txt

4) create .env

MONGO_URI=mongodb+srv://<user>:<password>@cluster0.rvxogsx.mongodb.net/dodotask?retryWrites=true&w=majority&appName=Cluster0
MONGO_DB=dodotask
JWT_SECRET=supersecret123
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

5) run 

python -m venv .venv
# Windows PowerShell:
. .venv/Scripts/Activate.ps1
# macOS/Linux:
# source .venv/bin/activate

python -m pip install -U pip wheel
pip install -r requirement.txt
uvicorn app.main:app --host 0.0.0.0 --port 1060
python -m uvicorn app.main:app --host 0.0.0.0 --port 1060 --reload
  ngrok http 1060