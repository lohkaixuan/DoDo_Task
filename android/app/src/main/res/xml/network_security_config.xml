<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) for dev. For production, restrict to specific domains. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
